<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alexpfragosoatgmail.mensageirows">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- android:theme="@style/AppTheme"> -->
        <activity android:name=".view.MainActivity"/>
        <activity android:name=".view.EnviarActivity" />
        <activity android:name=".view.LerActivity" />
        <activity android:name=".view.MostrarMensagensActivity" />
        <activity android:name=".view.NovoContatoActivity" />
        <activity android:name=".view.CadastrarNovoContatoActivity" />
        <activity android:name=".view.LerMensagensOrdenadasActivity" />
        <activity android:name=".view.MostrarMensagensOrdenadasActivity"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".view.TelaLoginActivity" >
            <intent-filter>
                 <action android:name="android.intent.action.MAIN" />

                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        </activity>>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".view.ContatosActivity"></activity>
    </application>

</manifest>